spring:
  data:
    mongodb:
      username: chirayu
      password: chirayu
      host: localhost
      port: 27017
      database: customer
      authentication-database: admin
  elasticsearch:
    uris: http://localhost:9200
server:
  port: 8090
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus, metrics
  endpoint:
    health:
      show-details: always
      show-components: always
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  observations:
    http:
      server:
        requests:
          ignore-patterns: "/actuator/**,/actuator"
  metrics:
    export:
      prometheus:
        enabled: true
keycloak:
  server-url: http://localhost:9098
  realm: micro-services
  client-id: micro-services-api
  client-secret: D6MYsd9gBhBRsF2tpRQEGr6tk9hanqHn
  admin-username: admin
  admin-password: admin
